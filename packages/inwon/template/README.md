# User templates

Place user-owned PowerPoint templates here for `template_com` mode.

Supported source extensions: `.pptx`, `.pptm`, `.potx`, `.potm`.

The MCP never overwrites a file in this folder. It creates a separate output presentation and changes visible text through Microsoft PowerPoint COM automation on Windows.
