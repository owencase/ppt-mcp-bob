# MCP 서버를 판단하는 축

이 레포는 같은 문제를 여러 명이 각자 풉니다. 그러면 "누구 게 더 좋냐"는 질문이
나오는데, 기준이 없으면 취향 싸움이 됩니다.

아래 여덟 개 축은 **서로의 구현을 리뷰할 때 쓰는 공통 언어**입니다.
전부 이 레포 안의 실제 코드를 예로 듭니다.

> 레퍼런스 구현(`reference/`)이 여러 축에서 단순한 쪽에 있는데, 그건 결함이
> 아니라 의도입니다. 읽기 쉬운 출발점이 필요해서 그렇게 만들었습니다.
> "레퍼런스가 이렇게 했으니 맞다" 고 생각하지 말고, 왜 그렇게 했는지 보세요.

---

## 1. Tool 의 고도(altitude)

**가장 중요한 축입니다.**

MCP tool 은 *라이브러리가 할 수 있는 것* 이 아니라 *모델이 하려는 것* 의
높이에 맞춰야 합니다.

```
낮음 (라이브러리 높이)          높음 (의도 높이)
add_text_box(left_cm, ...)  ←──────────→  create_deck(spec)
```

낮으면 좌표 계산이 LLM 의 일이 됩니다. 5장짜리 덱에 30번 호출해야 하고, 계산이
틀리면 글자가 슬라이드를 벗어납니다. 높으면 호출이 줄고 레이아웃 품질을 서버가
보장하지만, 유연성을 잃습니다.

| | 예 |
|---|---|
| 낮은 쪽 | `reference/mcp-server` — `add_text_box(file_path, slide_index, text, left_cm, top_cm, width_cm, height_cm, font_size_pt, bold, color_hex, align)` |
| 높은 쪽 | `projects/SeoJHeasdw` — `create_deck(spec, output_path)` 한 번 |

**리뷰할 때 물어볼 것**

- tool 인자에 좌표·픽셀·폰트 크기가 있는가? 있다면 그 계산은 누가 하나?
- 결과물 하나를 만드는 데 tool 을 몇 번 불러야 하나?
- 모델이 틀리기 쉬운 계산을 모델에게 시키고 있지 않은가?

정답은 없습니다. 다만 **어느 높이를 골랐는지 알고 골랐어야** 합니다.

---

## 2. MCP 의 세 요소를 쓰는가

대부분 tool 만 씁니다. MCP 에는 셋이 있습니다.

| | 뜻 | 예 |
|---|---|---|
| **tool** | 모델이 *실행* 하는 것 — 부작용 있음 | 파일 생성, 수정 |
| **resource** | 모델이 *읽는* 것 — 부작용 없음 | 테마 팔레트, 템플릿 목록 |
| **prompt** | 사용자가 *고르는* 잘 만든 시작점 | "주제로 덱 구조 잡기" |

테마 팔레트를 tool 로 만들면 모델이 *색을 알아내려고 동작을 실행* 해야 합니다.
resource 로 두면 그냥 읽습니다. **읽기와 쓰기를 구분하는 것** 이 핵심입니다.

- 예: `projects/SeoJHeasdw/src/ppt_mcp/server.py` 의 `theme://list`

**물어볼 것** — 내 tool 중에 부작용이 없는 게 있는가? 그건 resource 여야 합니다.

---

## 3. Tool 의 개수와 설명

`description` 은 **모델이 이 tool 을 언제 쓸지 판단하는 유일한 근거** 입니다.
사람이 읽는 주석이 아닙니다.

- tool 이 많을수록 모델이 헷갈립니다. 20개보다 5개가 낫습니다.
- 이름이 비슷한 tool 두 개는 거의 항상 잘못 골립니다.
- `description` 이 한 줄짜리 명사구면 모델이 추측하게 됩니다.

**물어볼 것** — tool 목록만 보고 각각 언제 쓰는지 구분되는가?

---

## 4. 에러가 고치는 법을 알려주는가

모델은 에러를 읽고 **다시 시도합니다.** 그래서 에러 메시지 품질이 곧 다음
시도의 성공률입니다.

```
✗  invalid input
✗  ValueError: text too long
✓  불릿은 한 장에 최대 6개입니다 (9개를 보냈습니다). 슬라이드를 나누세요.
✓  2번째 슬라이드(section): 14pt 로 줄여도 상자에 안 들어갑니다. 텍스트를 줄이세요.
✓  '없는테마' 테마는 없습니다. 사용 가능: marketing_warm, minimal_dark, ...
```

세 가지가 들어가야 합니다 — **무엇이**, **왜**, **어떻게 고치는지**.
여러 항목 중 하나가 문제면 **몇 번째인지** 까지.

- 예: `reference/ppt-bridge/bridge.py` 의 `dispatch()` — 알 수 없는 액션에
  사용 가능 목록을 함께 돌려줍니다
- 예: `projects/SeoJHeasdw/src/ppt_mcp/models.py`

**물어볼 것** — 이 에러를 받은 모델이 다음 시도에 성공할 수 있는가?

관련해서, **모르는 필드를 조용히 무시하지 마세요.** 모델이 잘못된 필드에 값을
실어 보내면 그 내용은 사라지는데 호출은 성공합니다. 모델은 자기가 뭘 잘못했는지
영원히 모릅니다. pydantic 이면 `extra="forbid"`.

---

## 5. 신뢰 경계 — LLM 이 준 값을 어디까지 믿는가

**tool 인자는 사용자 입력입니다.** 모델이 만들었을 뿐, 검증 없이 믿을 근거는
없습니다.

| 위험 | 최소 방어 |
|---|---|
| 파일 경로 | 정규화 후 허용 디렉터리 안인지 검사 (`..` 탈출 포함) |
| 외부 URL | scheme 허용목록 + 사설/루프백 IP 차단 (SSRF) |
| 크기·개수 | 상한 (슬라이드 수, 문서 크기, 다운로드 바이트) |

좋은 예가 레포 안에 있습니다.

- HTML 을 만드는 서버라면 `escapeHtml()` 이 빠지는 순간 인젝션입니다.
  슬라이드 제목에 `<script>` 를 넣는 건 사용자가 실수로도 합니다
- `projects/SeoJHeasdw` 의 `resolve_output_path()` — `PPT_MCP_OUTPUT_DIR`
  밖으로 못 씁니다
- 이전에 올라온 COM 기반 구현의 `_public_image_url()` — DNS 를 해석해서
  사설 IP 로 가는 URL 을 막습니다. SSRF 방어의 교과서적 예입니다

**물어볼 것** — 파일을 쓰는 지점이 몇 군데인가? 한 군데면 게이트도 한 군데면
됩니다. 여러 군데면 하나를 빼먹습니다.

---

## 6. 실패했을 때 무엇이 남는가

20번 호출짜리 작업이 8번째에서 실패하면?

- **레퍼런스 방식**: tool 호출마다 대상 파일을 덮어씁니다. 반쯤 만들어진
  `.pptx` 가 디스크에 남습니다.
- **원자적 방식**: 임시 파일에 다 쓴 뒤 `os.replace` 로 교체합니다. 실패하면
  아무것도 안 남습니다.

`os.replace` 는 같은 파일시스템 안에서만 원자적입니다. 임시 파일을 대상과
같은 디렉터리에 만드세요.

**물어볼 것** — 중간에 죽으면 사용자에게 뭐가 남는가?

---

## 7. stdout 규율 (stdio 서버라면)

**stdout 은 프로토콜 전용입니다.** 디버깅용 `print()` 하나가 JSON 을 깨고,
그때 클라이언트에 보이는 건 "Invalid JSON" 같은 무의미한 문자열입니다.
원인을 찾기가 아주 어렵습니다.

문서로 "print 쓰지 마세요" 라고 적는 건 약합니다. **구조적으로 불가능하게**
만드세요.

- 예: `reference/ppt-bridge/bridge.py` 의 `main()` — 핸들러가 도는 동안
  stdout 을 버퍼로 바꿔치기하고, 새어나온 출력은 stderr 로 옮깁니다.
  삼키지 않기 때문에 디버깅은 그대로 됩니다.

로그는 언제나 **stderr** 로.

---

## 8. 계약을 테스트하는가

MCP 서버에서 가장 잡기 어려운 실패는 **서버가 부팅 중에 죽는 것** 입니다.
클라이언트에는 그냥 "tool 이 없음" 으로 보입니다.

최소한 이 두 개는 있어야 합니다.

1. **노출 테스트** — 진짜 클라이언트로 붙어서 tool/resource/prompt 가 실제로
   보이는지. `reference/mcp-server/test/tools.test.mjs`
2. **계약 드리프트 테스트** — 정의가 두 곳에 나뉘어 있다면 둘이 어긋났는지.
   레퍼런스는 tool 을 `index.ts` 에, 핸들러를 `bridge.py` 에 두기 때문에
   한쪽만 고치면 조용히 깨집니다. 그걸 잡는 테스트가 들어 있습니다.

**테스트마다 "왜 이게 있는지" 주석을 남기세요.** 3개월 뒤 그 테스트가 깨졌을
때, 고쳐야 할지 지워야 할지 판단할 근거는 그 주석뿐입니다.

---

## 해 본 예시 — 레퍼런스 vs SeoJHeasdw

축만 있고 실제 비교를 한 번도 안 보여주면 감이 안 옵니다. 지금 레포에 있는
두 구현을 여덟 축으로 훑어 봅니다. **리뷰가 이런 모양이면 됩니다.**

| 축 | `reference/` | `projects/SeoJHeasdw` |
|---|---|---|
| 1. tool 고도 | 낮음 — 좌표를 호출자가 계산 | 높음 — `create_deck(spec)` 1회 |
| 2. 세 요소 | tool 만 (9개) | tool 3 · resource 2 · prompt 1 |
| 3. 개수·설명 | 9개, 설명 한 줄씩 | 3개, 언제 쓰는지까지 |
| 4. 에러 품질 | 빠진 파라미터 이름은 알려줌 | 고치는 법 + 몇 번째 슬라이드인지 |
| 5. 신뢰 경계 | 경로 검증 없음 | `PPT_MCP_OUTPUT_DIR` 게이트 |
| 6. 실패 원자성 | 호출마다 덮어씀 | 임시파일 → `os.replace` |
| 7. stdout | 버퍼 격리로 보호 | 해당 없음 (stdio 로그 없음) |
| 8. 계약 테스트 | 노출 + 드리프트 테스트 | 노출 + 설계 주장 고정 테스트 |

**그런데 이 표만 보고 "SeoJHeasdw 가 낫다" 고 읽으면 틀렸습니다.**

- 레퍼런스는 **읽기 쉬운 출발점**이 목적입니다. tool 하나가 python-pptx 호출
  하나에 대응해서, 처음 보는 사람이 무슨 일이 일어나는지 바로 압니다.
  `create_deck(spec)` 은 강력하지만 안에서 뭘 하는지 감춥니다.
- 낮은 고도는 **유연합니다.** 레퍼런스로는 아무 위치에 아무 도형이나 놓을 수
  있지만, `create_deck` 은 정해진 네 가지 슬라이드만 만듭니다. 자유형 레이아웃이
  필요하면 레퍼런스 쪽이 맞습니다.
- 레퍼런스의 7번(stdout 격리)은 두 언어를 오가는 구조라서 필요했던 방어입니다.
  단일 프로세스면 애초에 안 생기는 문제입니다.

**축은 점수표가 아니라 질문 목록입니다.** 다르게 했으면 "왜?" 를 묻고, 그 답이
설계 의도면 그걸로 된 겁니다. 답이 "생각 안 해봤는데" 면 거기가 배울 지점입니다.

---

## 리뷰 체크리스트

남의 `projects/` 를 볼 때 이 순서로 보면 빠릅니다.

```
□ tool 목록만 보고 각각 언제 쓰는지 구분되는가
□ tool 인자에 좌표·폰트 크기가 있는가 — 있다면 그 계산은 누가 하나
□ 결과물 하나에 tool 을 몇 번 불러야 하나
□ 부작용 없는 tool 이 있는가 — resource 여야 하지 않나
□ 에러 메시지를 받은 모델이 다음 시도에 성공할 수 있는가
□ 모르는 필드를 조용히 무시하지 않는가
□ 파일 경로 / 외부 URL 을 검증 없이 쓰는가
□ 중간에 실패하면 반쯤 만들어진 산출물이 남는가
□ stdout 에 로그가 섞일 수 있는가
□ 서버가 부팅해서 tool 을 노출하는지 확인하는 테스트가 있는가
```

리뷰는 **점수 매기기가 아니라 질문하기** 입니다. 위 항목 중 다르게 한 게
있으면 "왜 그렇게 했어?" 를 물어보세요. 대개 이유가 있고, 없으면 그때
같이 생각하면 됩니다.

---

## 내 프로젝트 README 에 뭘 써야 하나

코드는 무엇을 하는지 말해주지만 **왜 그렇게 했는지는 말해주지 않습니다.**
비교하며 배우는 게 목적이라면 그 '왜' 가 있어야 합니다.

최소한 이 세 가지는 적어 주세요.

```markdown
# <내 아이디>의 ppt-mcp

## 1. 어떤 선택을 했나
어느 축에서 무엇을 골랐는지. 예: "tool 고도를 높게 잡았다.
좌표 계산을 모델에게 시키면 자꾸 틀려서."

## 2. 어떻게 쓰나
설치 · 실행 · MCP 클라이언트 등록. 남이 돌려볼 수 있어야 합니다.

## 3. 안 한 것
의도적으로 뺀 기능과 그 이유. 이게 있으면 리뷰어가
"이건 왜 없어요?" 를 묻지 않아도 됩니다.
```

`projects/SeoJHeasdw/README.md` 가 이 형태입니다. 길게 쓸 필요는 없습니다 —
**선택과 이유가 보이면 됩니다.**

---

## 이 문서를 고치고 싶다면

축이 빠졌거나 예시가 낡았으면 PR 을 열어 주세요. 여러분의 구현에서 나온
좋은 예시를 여기에 추가하는 것도 환영합니다 — 그게 이 문서가 살아 있는
방법입니다.
